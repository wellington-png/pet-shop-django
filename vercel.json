{
    "builds": [{
        "src": "clinica/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "clinica/wsgi.py"
        }
    ]
}